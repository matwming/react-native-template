import React from 'react'
import { View, Text } from 'react-native'

const useNotification = () => {
    return (
        <View>
            <Text>this is custom notification hook</Text>
        </View>
    )
}

export default useNotification
