import React from 'react'
import { View, Text } from 'react-native'

const useFlatList = () => {
    return (
        <View>
            <Text>this is flat list custom hook</Text>
        </View>
    )
}

export default useFlatList
