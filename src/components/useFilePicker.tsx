import React from 'react'
import { View, Text } from 'react-native'

const useFilePicker = () => {
    return (
        <View>
            <Text>this is custom file picker. still building</Text>
        </View>
    )
}

export default useFilePicker
